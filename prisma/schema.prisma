// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://6h3x9guv7bw7e09fggh4:pscale_pw_Ve0CDGpsR1X9yMbndJXQotNhcqOAE8ibdOZQKrTON1F@aws.connect.psdb.cloud/msa?sslaccept=strict"
}

// Nullable이면 ?
model comment {
    // 댓글 고유 번호
    id Int @id @default(autoincrement())

    reviewId String

    /// 내용
    text String

    userId String

    // 댓글 작성일자
    createTime DateTime @default(now())

}